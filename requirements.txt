fastapi[standard]==0.115.12
uvicorn[standard]==0.34.2
sqlalchemy[asyncio]==2.0.40
asyncpg==0.30.0
pydantic==2.11.3
aiosqlite==0.21.0
# Removed (re-add when needed):
#   alembic              — will return when we replace LTP_RESET_DB with real migrations
#   python-dotenv        — Railway provides env vars directly; only needed for local .env files
#   pydantic-settings    — no settings classes in use
#   passlib[bcrypt]      — no password storage yet (will return with Google OAuth user table)
#   python-jose          — no JWTs yet (will return with Google OAuth)
#   python-multipart     — no file uploads yet
